class Movie {
  final String title, urlImage, description, rating;
  final List<String> category;
  Movie({
    required this.title,
    required this.urlImage,
    required this.description,
    required this.rating,
    required this.category,
  });
}
